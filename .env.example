# Yjeek Vendor & Admin Portal — environment example
# Copy to `.env` for local development. Change only VITE_API_BASE_URL for production.

# Backend API base (include /api/v1). No trailing slash required.
# Ready for when Vendor APIs are connected — does not affect UI while mocks are on.
VITE_API_BASE_URL=http://192.168.10.251:3000/api/v1

# Vendor Panel data source
# true  = mock data (default until you provide an API to wire)
# false = real backend (only after that endpoint is connected)
VITE_VENDOR_USE_MOCK_API=true

# Admin Panel: keep true until Admin API integration
VITE_ADMIN_USE_MOCK_API=true

# Request timeout in milliseconds
VITE_API_TIMEOUT_MS=30000

# Do not put passwords, access tokens, refresh tokens, or API keys in Vite env files.

testing branch