/** Max cards shown per column on Live / Scheduled / Pickup / Dine-in / Services board previews. */
export const ADMIN_BOARD_PREVIEW_LIMIT = 5

/** Max cards loaded for a column full-view (↗ link). */
export const ADMIN_BOARD_FULL_LIMIT = 100
